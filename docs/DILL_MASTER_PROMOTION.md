# Dill 67th master promotion

This helper performs the atomic promotion after evidence staging. It adds Dill (`Anethum graveolens`) to the master, updates 67=64+2+1 review accounting, wires addendum 10 into runtime/PWA, adds the sitemap entry, and regenerates static plant pages from all assessment addenda.

Safety scope remains `Tortoise_general`; seeds are excluded and the verdict must not be promoted to Mediterranean Testudo/Ibera direct evidence.
